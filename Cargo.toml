[package]
name = "cuda-snapshot"
version = "0.1.0"
edition = "2021"
description = "Point-in-time snapshots — state freezing, diff computation, rollback, and timeline management for agent state"

[dependencies]
serde = { version = "1", features = ["derive"] }
